namespace LeanCache.Server.Tests;

public class ServerStartupTests
{
    [Fact]
    public void Placeholder_ServerProjectCompiles()
    {
        // Verifies the Server project compiles and is referenceable.
        Assert.True(true);
    }
}